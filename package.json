{
  "name": "candidate-test",
  "private": true,
  "version": "1.0.0",
  "description": "Christian Egwim Doctor lib developer test.",
  "main": "doctorlibAPI.js",
  "scripts": {
    "db:migrate": "knex migrate:latest",
    "db:rollback": "knex migrate:rollback",
    "test": "yarn db:migrate && jest --watch"
  },
  "dependencies": {
    "knex": "^0.14.6",
    "moment": "^2.22.1",
    "sqlite3": "^4.0.6"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-module-resolver": "^3.1.1",
    "babel-preset-env": "^1.7.0",
    "jest": "^24.5.0"
  }
}